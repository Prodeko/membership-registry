RUST_LOG=debug
DATABASE_URL=postgres://membership:secret@localhost:5433/membership?sslmode=disable

# On linux (WSL) you can't bind to ports below 1024 without
# net_bind_service capability or root.
# Port 80 would cause tokio::net::TcpListener::bind(PORT) to fail with
# Os { code: 13, kind: PermissionDenied, message: "Permission denied" }
PORT=8080
FRONTEND_URL=http://127.0.0.1:5173

# ORY_BASE_URL is the base URL of the ORY Kratos instance.
ORY_BASE_URL=http://127.0.0.1:4455/.ory/kratos

# OAUTH_CLIENT_ID and OAUTH_CLIENT_SECRET is the client ID and secret 
# of the OAuth2 client that is used to authenticate users.
# In dev env, create a new OAuth2 client in ORY Hydra and use the client and secret here.
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URL=http://127.0.0.1:80/auth/callback
OAUTH_ISSUER_URL=http://127.0.0.1:4444

STRIPE_ENDPOINT_SECRET=whsec_937f053ed2a8cc24b3d7e4dd2ee65f693cbb61559c659f17e75b5414f5d90f4d